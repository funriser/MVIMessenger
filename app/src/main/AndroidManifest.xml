<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.funrisestudio.buzzmessenger">

    <application
        android:name=".BuzzApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BuzzMessenger">
        <service
            android:name=".data.messages.MessengerService"
            android:enabled="true"
            android:exported="false"/>

        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.BuzzMessenger.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.messenger.MessengerActivity"
            android:allowEmbedded="true"
            android:label="@string/app_name"
            android:resizeableActivity="true" />
    </application>

</manifest>